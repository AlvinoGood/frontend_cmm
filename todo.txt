POST {{BASE_URL}}/medical-center/v1/encounters
Description: Create an encounter between the authenticated provider and a patient by DNI
Token: medical
Authorization: Bearer {{MEDICAL_TOKEN}}
Content-Type: application/json
{
  "patientDni": "12345678"
}

---
CREATE TABLE encounters (
  encounters_id  BIGSERIAL PRIMARY KEY,
  provider_id    BIGINT NOT NULL REFERENCES users(user_id) ON UPDATE CASCADE,  -- médico u otro proveedor
  patient_id     BIGINT NOT NULL REFERENCES users(user_id) ON UPDATE CASCADE,  -- paciente
  created_at     TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX encounters_provider_idx ON encounters(provider_id);
CREATE INDEX encounters_patient_idx  ON encounters(patient_id);

---

GET {{BASE_URL}}/medical-center/v1/users/lookup/12345678


Example of return after to call API "GET {{BASE_URL}}/medical-center/v1/users/lookup/12345678" and the user exist in the database:
{
    "success": true,
    "data": {
        "exists": true,
        "dni": "16161616",
        "email": "testting06@gmail.com"
    },
    "timestamp": "2025-11-17T01:50:33.494Z"
}

---
Example of return after to call API "GET {{BASE_URL}}/medical-center/v1/users/lookup/12345678" and the user exist doesn't exist in the database:
{
    "success": true,
    "data": {
        "exists": false
    },
    "timestamp": "2025-11-17T01:51:07.142Z"
}